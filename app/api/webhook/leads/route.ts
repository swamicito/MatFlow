// Generic leads webhook — same handler as /api/webhook/webflow.
// Use this URL in your contact forms. The /api/webhook/webflow alias
// is kept for backwards compatibility with any existing integrations.
export { POST } from "@/app/api/webhook/webflow/route";
